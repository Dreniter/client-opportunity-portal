# Setup

1. Keep `config.js` as provided.
2. Upload the files in this folder to the root of the GitHub repository.
3. Do not rerun the Supabase SQL if the existing database is already working.
4. Open the GitHub Pages site and hard-refresh it after deployment.
